#ifndef __INCLUDE_PCRE_COMPAT_WRAPPER_H__
#define __INCLUDE_PCRE_COMPAT_WRAPPER_H__

/*
 * Public include wrapper.
 * Keep compatibility logic in lib/pcre_compat.h while allowing non-lib
 * modules to include this header via the standard include search path.
 */
#include "../lib/pcre_compat.h"

#endif
